[pytest]
testpaths = tests
# `production` is the environment that exists on the Qualflare project this
# uploads to. The server matches an environment by uid, not by display name, so
# an unknown value is rejected outright and the whole launch is lost.
qualflare_environment = production
qualflare_output_dir = e2e-results
